// Generated by CoffeeScript 1.3.3
var app, assets, express, fluidity, fs, port, publicDir, stylus;

express = require('express');

assets = require('connect-assets');

stylus = require('stylus');

fluidity = require('fluidity');

fs = require('fs');

publicDir = "./public";

app = express();

app.use(assets());

app.use("/images", express["static"]('./public/images'));

app.set("view engine", "jade");

app.set('view options', {
  debug: true,
  pretty: true
});

app.get('/', function(req, res) {
  return res.render('index');
});

app.get('/two-column-example', function(req, res) {
  return res.render('two_column_example');
});

app.get('/three-column-example', function(req, res) {
  return res.render('three_column_example');
});

app.get('/three-column-fixed-example', function(req, res) {
  return res.render('three_column_example_fixed');
});

port = process.env.PORT || process.env.VMC_APP_PORT || 3000;

app.listen(port, function() {
  return console.log("Listening on " + port + "\nPress CTRL-C to stop server.");
});
